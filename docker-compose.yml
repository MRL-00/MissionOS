services:
  the-office:
    build: .
    container_name: the-office
    restart: unless-stopped
    ports:
      - "5173:5173"
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - office-data:/app/data
volumes:
  office-data:
