# Hermes

Hermes is scaffolded but intentionally stubbed.

MissionOS notes that Hermes manages its own `SOUL.md` and profile files externally.

The adapter exists so the UI and DB can represent Hermes agents without pretending the runtime is implemented.
