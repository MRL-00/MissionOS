# Settings

Settings manages:

- Local profile details
- Password changes
- Linear credentials
- Per-engine configuration
- Full project reset

Resetting the project deletes the SQLite file and returns MissionOS to first-run state.
